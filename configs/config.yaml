# Hyperparameters for fake news classification training
# Override via CLI: uv run python src/project/train.py seed=123 epochs=5

# Reproducibility
seed: 42

# Training
epochs: 10
batch_size: 64
train_split: 0.8
limit_train_batches: 1.0
log_every_n_steps: 10

# Model
model_name: "distilbert-base-uncased"
lr: 2e-5
max_length: 256

# Paths (relative to project root)
data_path: "src/project/data"
processed_data_path: "src/project/data/processed/news.csv"
save_dir: "src/project/models"

# Checkpointing
checkpoint_path: null
save_top_k: 1

# Wandb
wandb_project: "news-classification"
wandb_entity: null
